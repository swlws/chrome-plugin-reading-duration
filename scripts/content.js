const headerElementList = document.querySelectorAll("div");

for (let i = 0; i < headerElementList.length; i++) {
    const headerElement = headerElementList[i];
    headerElement.style.color = "orange";
}
